name: rms_logistics
version: 1.0.0+1
description: Logistics marketplace connecting customers, drivers, companies.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  dio: ^5.3.2
  google_maps_flutter: ^2.5.0
  socket_io_client: ^2.0.3+1
  firebase_core: ^2.20.0
  firebase_messaging: ^14.6.6
  firebase_storage: ^11.2.7
  image_picker: ^1.0.7
  flutter_svg: ^2.0.7
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
